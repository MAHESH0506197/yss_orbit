# yss_orbit\backend\api\v1\urls.py
"""
API v1 URL Configuration.
(This is an alternative entry point to config/urls.py if apps are mounted directly via the api app).
For now, we route it directly or keep it empty as config/urls.py acts as the master.
"""
from django.urls import path

urlpatterns = [
    # Endpoints could be registered here
]
