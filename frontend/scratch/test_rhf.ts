import { useForm } from 'react-hook-form';
import { useEffect } from 'react';

// Just a quick check to see if we can log something or we know it's undefined.
// We can't really run react-hook-form easily in node without setup.
