// yss_orbit\frontend\src\design_system\themes\baseTheme.ts
import { colors } from '../tokens/colors';
import { typography } from '../tokens/typography';

export const baseTheme = {
  colors: {
    ...colors,
  },
  typography: {
    ...typography,
  },
  // Default values before tenant overrides
};
