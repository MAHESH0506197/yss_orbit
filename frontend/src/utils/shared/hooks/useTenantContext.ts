// yss_orbit\frontend\src\shared\hooks\useTenantContext.ts
import { useContext } from 'react';
// Assuming TenantContext is implemented elsewhere
export const useTenantContext = () => {
  return { tenantId: 'default-tenant' };
};
